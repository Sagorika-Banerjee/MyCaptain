#include<stdio.h>
int main()
{
    printf("Enter 2 no.s a and b: ");
    int a, b;
    scanf("%d %d", &a, &b);
    a = a + b;
    b = a - b;
    a = a - b;
    printf("After swapping, a= %d and b= %d\n", a,b);
    return 0;
}
